class Solution {
public:
    striclass Solution {
public:
    string addBinary(string a, string b) {
        string ans;
class Solution {
public:
    string addBinary(st[i] - '0' : 0) + (j >= 0 ? b[j] - '0' : 0);
            ans.push_back((carry % 2) + '0');
            carry /= 2;
        }
        reverse(ans.begin(), ans.end());
        return ans;
    }
};
    }
};
