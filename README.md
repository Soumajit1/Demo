/**
 * Definition for singly-linked list.
  struct ListNode {
     int val;
      stNode *next;
   ListNode() : va
    ListNode* rotat
        while (k--) {
            fast = fast->next;
        }
        return ans;
    }
};
